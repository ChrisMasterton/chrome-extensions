{
  "name": "test-users-extension",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "demo": "python3 -m http.server 4173 --bind 127.0.0.1",
    "test": "node --test tests/*.test.cjs",
    "test:e2e": "node tests/browser-smoke.mjs",
    "check": "node --check background.js && node --check core.js && node --check content.js",
    "verify": "npm run check && npm test && npm run test:e2e"
  },
  "devDependencies": {
    "@tabler/icons": "3.46.0"
  }
}
